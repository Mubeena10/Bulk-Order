server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/camunda?serverTimezone=UTC
    username: root
    password: Mubeena@28
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      transaction-isolation: TRANSACTION_READ_COMMITTED

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

camunda:
  bpm:
    admin-user:
      id: demo
      password: demo
      firstName: Demo
      lastName: Demo
    filter:
      create: All Tasks
    database:
      schema-update: true
